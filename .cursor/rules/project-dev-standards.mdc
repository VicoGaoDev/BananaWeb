description: 项目全局通用规范
alwaysApply: true
---

# 项目全局规则

1. 如果读了且执行这个规则文件，就在输出时先称呼我为高道友。
2. 先做最小改动，优先修当前需求，不顺手大改无关模块，不随意重构已有稳定流程。
3. 如果需求涉及表改动，例如新增表、修改字段、补索引、迁移历史数据，先问询我是否要先进入计划阶段，再决定执行；不要直接先改代码。
4. 任何改动如果可能影响通用任务链路（如 `tasks / images / worker / history`），必须优先做兼容，避免影响现有稳定流程。
5. 写代码和文案都尽量直接、朴素、可维护；能复用现有模式就复用，不重复发明一套新结构。
6. 改动完成后要做定向验证；如果没验证，要明确说明原因和风险。

## 例子

- 好例子：先确认影响范围，再做兼容式修改，最后补验证结果。
- 坏例子：为赶进度直接大改无关模块，或改完不说明是否验证。
